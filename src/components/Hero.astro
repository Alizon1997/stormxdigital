---
import Container from "./Container.astro"
import { getRelativeLocaleUrl } from 'astro:i18n';

const currentLocale = Astro.currentLocale || 'it';

// Import translations
import enTranslations from '../../public/locales/en/translation.json';
import itTranslations from '../../public/locales/it/translation.json';

const translations = currentLocale === 'en' ? enTranslations : itTranslations;
const collaborators = ['a.png', 'b.png', 'c.png', 'd.png', 'e.png', 'f.png','g.png','h.png'];

---

<Container>
  <div class="pt-10 flex flex-col-reverse lg:flex-row justify-center items-center" id="hero">
    <div class="w-auto py-4 sm:py-5 md:py-10 lg:py-20 lg:pl-14">
      <div class="text-left space-y-2 sm:space-y-3 md:space-y-4">
        <h1 class=" mt-10 md:mt-0 text-[28px] font-semibold sm:text-[36px] md:text-[38px] lg:text-[40px] xl:text-[48px] leading-tight">
          {translations.hero.title1}<br/>
          {translations.hero.title2}<br/>
          <span class="bg-gradient-to-r from-[#F95C04] via-7% to-[#FF8E0A] to-82% text-white px-2 py-1 inline-block mt-2 rounded-lg">{translations.hero.title3}</span>
        </h1>
        <!-- Hero image for small screens -->
        <div class="lg:hidden flex  w-full h-full py-2 sm:py-12 md:py-12 justify-center item-center">
          <div class="relative max-w-sm sm:max-w-md mx-auto">
            <img src="/hero_img.png" alt="Amazon Dashboard" class="w-[200px] h-[230px] sm:w-[300px] sm:h-[330px] object-cover" />
          </div>
        </div>
        <p class="text-base sm:text-lg text-gray-700 mt-4 sm:mt-5 md:mt-6 mb-2 sm:mb-3 md:mb-4">
          {translations.hero.description1}<br class="hidden sm:inline"/>
          {translations.hero.description2}<br class="hidden sm:inline"/> 
          {translations.hero.description3}<br class="hidden sm:inline"/> 
          {translations.hero.description4}<br class="hidden sm:inline"/>
          {translations.hero.description5}
        </p>
        <div class="flex flex-col sm:flex-row items-start space-y-4 sm:space-y-0 sm:space-x-4">
          <a 
            href="https://cal.com/marcello-di-pierro/amazon-growth-session"
            class="inline-block text-center w-full md:w-auto sm:px-24 md:px-9 py-3 sm:py-4 md:py-5 bg-gradient-to-r from-[#F95C04] via-7% to-[#FF8E0A] to-82% text-white font-bold text-base sm:text-lg rounded-xl sm:rounded-2xl hover:bg-orange-700 transition duration-300"
          >
            {translations.hero.ctaButton}
          </a>
          <div class="flex flex-col space-y-1 sm:space-y-2">
            <div class="flex items-center space-x-2">
              <img src="/contatto.png" alt={translations.hero.feature1} class="w-4 sm:w-5 h-4 sm:h-5" />
              <span class="text-xs sm:text-sm">{translations.hero.feature1}</span>
            </div>
            <div class="flex items-center space-x-2">
              <img src="/approccio.png" alt={translations.hero.feature2} class="w-4 sm:w-5 h-4 sm:h-5" />
              <span class="text-xs sm:text-sm">{translations.hero.feature2}</span>
            </div>
            <div class="flex items-center space-x-2">
              <img src="/europa.png" alt={translations.hero.feature3} class="w-4 sm:w-5 h-4 sm:h-5" />
              <span class="text-xs sm:text-sm">{translations.hero.feature3}</span>
            </div>
          </div>
        </div>
      </div>
      <div class="flex items-center mt-6 sm:mt-8 space-x-3 sm:space-x-4">
        <img src="/amazon_ads.png" alt="Amazon Ads Verified Partner" class="h-16 sm:h-20 md:h-24 object-contain" />
        <img src="/amazon_spn.png" alt="Amazon SPN Certified" class="h-12 sm:h-14 md:h-16 object-contain" />
      </div>
    </div>
    <!-- Hero image for large screens -->
    <div class="hidden px-4 lg:flex w-full lg:w-1/2 py-12 sm:py-4 md:py-10 flex-col items-center lg:items-center">
      <div class="relative w-full max-w-sm sm:max-w-md">
        <img src="/hero_img.png" alt="Amazon Dashboard" class="w-full h-[616px] object-cover" />
      </div>
    </div>
  </div>
  
<!-- Collaborators Banner -->
<div class="w-full py-4 sm:py-6 md:py-8 lg:py-12 xl:py-16 mt-6 sm:mt-8 md:mt-10 lg:mt-2 xl:mt-2">
  <div class="container px-4 sm:px-6 md:px-8">
    <div class="grid grid-cols-3 sm:grid-cols-3 md:grid-cols-3 lg:flex lg:flex-wrap xl:flex-nowrap justify-center items-center gap-3 sm:gap-4 md:gap-6 lg:gap-10 xl:gap-12">
      {collaborators.map((logo, index) => (
        <div class="flex justify-center items-center p-2 sm:p-3 md:p-4">
          <img 
            src={`/${logo}`} 
            alt={`Collaborator ${index + 1}`} 
            class="w-full max-w-[90px] sm:max-w-[100px] md:max-w-[120px] lg:max-w-[100px] xl:max-w-[120px] h-auto object-contain"
          />
        </div>
      ))}
    </div>
  </div>
</div>
</Container>